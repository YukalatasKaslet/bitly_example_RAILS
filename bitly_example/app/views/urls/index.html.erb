<h1>Index URLS</h1>
<br>
<% if @urls %>
  <ul>
    <% @urls.each do |url| %>
      <li>
        <%= link_to url.short_url, user_url_path(params[:user_id],url.id) %>
      </li>
    <% end %>
  </ul>
<% end %>