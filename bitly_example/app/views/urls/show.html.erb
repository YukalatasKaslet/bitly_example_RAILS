<h1> Vista urls/:id </h1>

<% if @url %>
  <p>
    Short&ensp;url:&ensp;<%= link_to(@url.short_url,{:action => 'short_url', :short_url => @url.short_url }) %>
    <br>
    Long&ensp; url:&ensp;<%= @url.long_url %>
    <br>
    Visits:&ensp;<%= @url.visits_count %>
  </p>
  <%= link_to("Editar",{:action => 'edit', :id => @url.id }) %>&ensp;
  <%= link_to "Eliminar", @url, method: :delete %>
<% else %>
<strong> URL no válida o no existe</strong>
<% end %>